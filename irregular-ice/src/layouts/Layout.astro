---
interface Props {
	title: string;
}

---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Krypto St" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Kriptographic</title>
	</head>
	<body>
		<slot />
	</body>
	<div class="lights">
		<div class="light top-right"></div>
		<div class="light middle-left"></div>
		<div class="light bottom-right"></div>
	</div>
</html>
<style is:global>
	*{
		margin: 0;
		box-sizing: border-box;
	}
	:root {
		--accent: 136, 58, 234;
		--accent-light: 224, 204, 250;
		--accent-dark: 49, 10, 101;
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
	}
	html {
		font-family: system-ui, sans-serif;
		background: #09090E;
		background-size: 224px;
	}
	html,body {
		height: 100%;
		width: 100%;
	}
	code {
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			Liberation Mono,
			DejaVu Sans Mono,
			Bitstream Vera Sans Mono,
			Courier New,
			monospace,
			Poppins,
			Medium;
	}
	.lights {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
    }
	.light{
		position: absolute;
		width:200px;
		height: 192px;
		border-radius: 50%;
        background-color: rgba(136, 243, 235, 0.3);
		filter:blur(70px);
	}
	.top-right{
        top: -80px;
        right: -60px;
	}
	.middle-left {
        top: 40%;
        left: -100px;
    }
    .bottom-right {
        bottom: -50px;
        right: -100px;
    }
	/* Register your custom font family and tell the browser where to find it. */
	@font-face {
		font-family: 'Medium';
		src: url('/fonts/Medium.otf');
		font-weight: normal;
		font-style: normal;
		font-display: swap;
	}
</style>
